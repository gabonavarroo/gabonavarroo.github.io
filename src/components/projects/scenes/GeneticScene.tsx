'use client';
import type { SceneProps } from './types';
import { SceneSkeleton } from '../SceneSkeleton';

export default function GeneticScene({ inView: _inView }: SceneProps) {
  return <SceneSkeleton label="GA OPTIMIZER // CODEX PENDING" />;
}
