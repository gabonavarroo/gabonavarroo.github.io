'use client';
import type { SceneProps } from './types';
import { SceneSkeleton } from '../SceneSkeleton';

export default function OptionsScene({ inView: _inView }: SceneProps) {
  return <SceneSkeleton label="LIVE OPTIONS FLOW // CODEX PENDING" />;
}
