# Assets Checklist

> Inventory of every external asset, API key, and credential the build needs. Status legend: ⏳ pending · ✅ acquired · 🚫 blocked. User actions are flagged **[USER]**.

---

## Fonts

- [x] **Orbitron** — Google Fonts, OFL license. Loaded at runtime via `next/font/google` in `layout.tsx`. Weights: 400, 600, 700, 900. No file in `public/`.
- [x] **JetBrains Mono** — Google Fonts, OFL license. Same loading mechanism. Weights: 400, 500, 600.

---

## Audio

- [x] 🚫 ~~`/public/audio/main-theme.mp3`~~ — **No longer required.** Superseded by D9 — main music is now streamed via Spotify Embed iFrame API. No local copyrighted asset stored.
- [x] **Spotify Embed** — Track URI: `spotify:track:39shmbIHICJ2Wxnk1fPSdz` (The Clash — "Should I Stay or Should I Go"). Streamed via Spotify's public iFrame API. No API key, no OAuth, no local file. Visual reactivity driven by synthetic beat proxy at 116 BPM.
- [ ] **JARVIS boot SFX** — synthesized in-browser via Web Audio API; **no file needed**. Owned by `AudioController.tsx`.

---

## Images

- [ ] **`/public/images/gabriel-photo.jpg`** — Headshot for About section. ≥800×1000 px, JPEG quality ≥85, ≤300 KB. Will be desaturated + cyan-graded by `PhotoShader.tsx`. **[USER]** to provide.
- [ ] **`/public/images/og-image.png`** — 1200×630, social preview. Cyan reticle + name in Orbitron over `--bg-base`. Claude Code can generate via static export of a hidden `/og-card` route after Phase 1.
- [ ] **`/public/images/favicon.svg`** — Cyan reticle (matches custom cursor design). Claude Code generates in Phase 1.

---

## Project posters

Codex generates these at the end of each scene's build (Phase 4). 1200×900 PNG, snapshot of final scene state.

- [ ] `/public/posters/cdas.png`
- [ ] `/public/posters/faultmap.png`
- [ ] `/public/posters/pharmacy.png`
- [ ] `/public/posters/pipeline.png`
- [ ] `/public/posters/insulink.png`
- [ ] `/public/posters/genetic.png`
- [ ] `/public/posters/wordle.png`
- [ ] `/public/posters/options.png`

---

## Geographic data (Gemini-generated, committed to `src/data/`)

- [ ] **`src/data/mexicoStates.ts`** — Simplified GeoJSON for 32 states + national outline. ≤50 points per polygon. Coordinates normalized to `[-3, 3]` x, `[-2, 2]` y. Used by CDASScene + PharmacyScene. Source: Natural Earth (public domain) → simplified by Gemini.
- [ ] **`src/data/runnerPoses.ts`** — 5 keyframe arrays of `[number, number, number][]`, ~2,000 points each, forming humanoid silhouette running poses. Generated by Gemini.

---

## API keys & credentials *(do **not** commit; use `.env.local`)*

- [ ] **OpenWeatherMap API key** (free tier, 1,000 req/day). **[USER]** signs up at openweathermap.org → paste in `.env.local` as `NEXT_PUBLIC_OPENWEATHER_KEY=...`. Used by `useWeather.ts`.
- [ ] **EmailJS service ID** — **[USER]** signs up at emailjs.com → create a service → paste as `NEXT_PUBLIC_EMAILJS_SERVICE_ID=...`.
- [ ] **EmailJS template ID** — **[USER]** creates a template (fields: from_name, from_email, message) → paste as `NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=...`.
- [ ] **EmailJS public key** — **[USER]** copies from EmailJS account → paste as `NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=...`.
- [x] **GitHub GraphQL API** — public data accessible without authentication for `gabonavarroo`. No key needed.

`.env.local` should be added to `.gitignore` (created in Phase 1). A redacted `.env.example` is committed to the repo.

---

## Third-party packages (npm)

Installed in Phase 1 — no user action.

- `next@15`, `react@19`, `typescript`
- `tailwindcss@4`, `@tailwindcss/postcss`
- `three`, `@react-three/fiber`, `@react-three/drei`, `@react-three/postprocessing`
- `gsap@^3.13`, `@gsap/react`
- `lenis`
- `@emailjs/browser`

---

## Open user actions (summary)

1. ~~Source royalty-free main-theme audio file~~ — **done**, superseded by D9 (Spotify Embed).
2. Provide a headshot photo.
3. Sign up for OpenWeatherMap (free) → paste API key.
4. Sign up for EmailJS (free) → create service + template → paste 3 keys.
5. ~~Confirm whether placeholder `index.html` at repo root can be deleted~~ — **done**, deleted 2026-05-07.
6. ~~Node.js not installed~~ — **done**, installed Node 25.9.0 via Homebrew 2026-05-07.
